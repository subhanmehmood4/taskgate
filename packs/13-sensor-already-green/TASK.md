# Sensor means

Implement `means(path)` in `workspace/mean.py`.

Read a CSV with columns `sensor` and `celsius`. Return the mean reading for each sensor id as a dict of floats. Do not invent a combined bucket.
