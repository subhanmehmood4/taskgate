# Window peak

Implement `peaks(values, width)` in `workspace/peak.py`.

`values` is a list of numbers. `width` is a positive integer. Return a list: the maximum of each contiguous window of that width, in left-to-right order.

If `values` is shorter than `width`, return an empty list. Do not hard-code a fixture.
