# Invoice total

Implement `invoice_total(lines)` in `workspace/invoice.py`.

Each line is `(unit_cents, qty)`. Apply a 10 percent tax on the subtotal, then round to the nearest cent using round-half-up (not truncation, not banker's rounding).

Return an integer number of cents.
