# Timestamp parse

Implement `parse_stamp(text)` in `workspace/parse.py`.

Take a naive ISO-8601 datetime string (no offset) and return a `datetime.datetime`.
Do not invent fields that are not in the string.
